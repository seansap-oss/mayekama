import UIKit

// Build 1 scaffold. Build later in Xcode as a Custom Keyboard Extension.
class KeyboardViewController: UIInputViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
        // Build 5+ target: keyboard rows, suggestion strip, local language pack.
    }
}
